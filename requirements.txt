# Core app and UI
# Pinned: matches the locally validated version. Unpinned ">=1.28" made cloud deploys
# install a newer Streamlit whose Starlette-based server crashed on a starlette
# version mismatch (GZipResponder 'thread_minimum_size' TypeError).
streamlit==1.48.1
plotly>=5.17

# Data and numerics
pandas>=2.0
numpy>=1.24
scipy>=1.10
pyarrow>=12.0
openpyxl>=3.1

# ML / utils
scikit-learn>=1.3
joblib>=1.3

# Config
PyYAML>=6.0
